query LoginUser($email: String!, $password: String!) {
    LoginUser(user: {
        email: $email,
        password: $password
    }) {
        user {
            id
            name
            email
        }
        token
    }
}